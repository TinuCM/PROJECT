╔═══════════════════════════════════════════════════════════════════════╗
║                   APPLICATION STARTUP FLOWCHART                       ║
╚═══════════════════════════════════════════════════════════════════════╝


┌─────────────────────────────────────────────────────────────────────┐
│                         OPTION 1: ONE-CLICK                         │
│                     (Recommended for beginners)                     │
└─────────────────────────────────────────────────────────────────────┘

                    Double-click "start.bat"
                              │
                              ↓
                 ┌────────────────────────┐
                 │  Backend starts first  │
                 │   (Port 8000)          │
                 └────────────┬───────────┘
                              │
                              ↓ (waits 3 seconds)
                              │
                 ┌────────────────────────┐
                 │  Frontend starts       │
                 │   (Port 3000)          │
                 └────────────┬───────────┘
                              │
                              ↓ (waits 2 seconds)
                              │
                 ┌────────────────────────┐
                 │  Browser opens         │
                 │  http://localhost:3000 │
                 └────────────┬───────────┘
                              │
                              ↓
                     ✅ APP RUNNING!


┌─────────────────────────────────────────────────────────────────────┐
│                      OPTION 2: MANUAL START                         │
│                     (For advanced users)                            │
└─────────────────────────────────────────────────────────────────────┘

    Terminal 1                           Terminal 2
    ──────────                           ──────────
        │                                     │
        ↓                                     │
  [Activate venv]                             │
        │                                     │
        ↓                                     │
  [Start Backend]                             │
        │                                     │
        ↓                                     │
  uvicorn app.main:app --reload               │
        │                                     │
        ↓                                     ↓
  ✅ Backend Ready                      [Navigate to prj]
  (localhost:8000)                            │
                                              ↓
                                        [Start Frontend]
                                              │
                                              ↓
                                        npm run dev
                                              │
                                              ↓
                                        ✅ Frontend Ready
                                        (localhost:3000)


┌─────────────────────────────────────────────────────────────────────┐
│                      STARTUP SEQUENCE                               │
└─────────────────────────────────────────────────────────────────────┘

1. Backend Server (FastAPI)
   ↓
   • Reads config.py
   • Connects to app.db (SQLite)
   • Creates tables if needed
   • Starts uvicorn server
   • Listens on port 8000
   • ✅ Ready: "Application startup complete"

2. Frontend Server (React + Vite)
   ↓
   • Reads vite.config.js
   • Loads React components
   • Starts development server
   • Enables Hot Module Replacement
   • Listens on port 3000
   • ✅ Ready: "Local: http://localhost:3000/"


┌─────────────────────────────────────────────────────────────────────┐
│                      WHAT HAPPENS WHEN?                             │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│ Backend      │ Creates database tables
│ Starts       ├─ Loads JWT configuration
│              └─ Enables CORS for localhost:3000
└──────┬───────┘
       │
       ↓ 3 seconds
       │
┌──────────────┐
│ Frontend     │ Reads AuthContext.jsx
│ Starts       ├─ Sets API_BASE_URL to localhost:8000
│              └─ Checks localStorage for saved token
└──────┬───────┘
       │
       ↓ 2 seconds
       │
┌──────────────┐
│ Browser      │ Loads http://localhost:3000
│ Opens        ├─ Shows landing page
│              └─ Ready for signup/login
└──────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    VERIFICATION CHECKLIST                           │
└─────────────────────────────────────────────────────────────────────┘

□ Backend Terminal shows:
  "INFO: Application startup complete"

□ Frontend Terminal shows:
  "Local: http://localhost:3000/"

□ http://localhost:8000 shows:
  {"message":"AI Shopping Assistant API is running"}

□ http://localhost:8000/docs shows:
  Swagger UI interface

□ http://localhost:3000 shows:
  Your React application

✅ All checked? You're ready to go!


┌─────────────────────────────────────────────────────────────────────┐
│                         SHUTDOWN PROCESS                            │
└─────────────────────────────────────────────────────────────────────┘

Backend Terminal:          Frontend Terminal:
     CTRL + C                  CTRL + C
         ↓                          ↓
   Server stops               Server stops
         ↓                          ↓
   Port 8000 freed           Port 3000 freed


⚠️  Database (app.db) is NOT affected
    All your data persists!


┌─────────────────────────────────────────────────────────────────────┐
│                         RESTART PROCESS                             │
└─────────────────────────────────────────────────────────────────────┘

Just run the same commands again:
• Double-click start.bat
  OR
• Run the manual commands in both terminals

Your data will still be there! 🎉


═══════════════════════════════════════════════════════════════════════
                     TROUBLESHOOTING GUIDE
═══════════════════════════════════════════════════════════════════════

❌ Backend won't start:
   → Check if port 8000 is already in use
   → Activate virtual environment first
   → Install dependencies: pip install -r requirements.txt

❌ Frontend won't start:
   → Check if port 3000 is already in use
   → Navigate to correct folder: cd ai-project\prj
   → Install dependencies: npm install

❌ "Module not found" errors:
   Backend → pip install -r requirements.txt
   Frontend → npm install

❌ "CORS error" in browser:
   → Make sure backend is running on port 8000
   → Frontend must be on port 3000
   → Check AuthContext.jsx has correct API_BASE_URL

═══════════════════════════════════════════════════════════════════════

                    🎉 HAPPY CODING! 🚀

═══════════════════════════════════════════════════════════════════════

